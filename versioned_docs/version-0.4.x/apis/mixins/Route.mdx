---
id: Route
title: Route
sidebar_label: Route
slug: /apis/mixins/Route
description: The typehint of the route provided by selected_path.
---

import APITopBar, { IconObjType } from "@site/src/components/APITopBar";

<APITopBar type="type" source="mixins.Route" />

```python
Route = Sequence[str | int | Sequence[str | int]]
```

`Route` is a `Union` of `str`, `int`, and `Sequence[str | int]`.

`Route` is used for annotating the route (also called `keyPath`) returned from the
`selected_path` property of the component.
